import 'package:flutter/material.dart';

class HostoryView extends StatelessWidget {
  const HostoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "History",
        style: TextStyle(fontSize: 35, color: Colors.black),
      ),
    );
  }
}
